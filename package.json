{
  "name": "consultor-rh",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend run prisma:generate && npm --prefix frontend ci && npm --prefix frontend run build",
    "dev": "./start.sh",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
